/**
 * Re-export do saneador canonical que vive em `src/utils/sanitize.ts`.
 * Mantido aqui para compatibilidade com imports existentes no catálogo.
 */
export { sanitizeCatalogText, safeSpacing, deepSanitize } from '../../utils/sanitize';
